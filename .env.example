# Node Environment (development or production)
NODE_ENV=production

# Port (Railway will provide this automatically)
PORT=3001

# Signal API URL (optional - for Signal integration)
# If using Docker on Railway, this would be the internal Docker service URL
SIGNAL_API_URL=http://localhost:8080
