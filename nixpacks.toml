[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "cd client && npm install && cd .."
]

[phases.build]
cmds = [
  "npm run build",
  "cd client && npm run build && cd .."
]

[start]
cmd = "node dist/server.js"

[variables]
NODE_ENV = "production"
