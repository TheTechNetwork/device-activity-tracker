name: Build and Test

on:
  pull_request:
    branches:
      - main
  push:
    branches-ignore:
      - main
      - claude/deploy-cloudflare-workers-Wkmno

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install root dependencies
        run: npm ci

      - name: Install client dependencies
        run: cd client && npm ci

      - name: Build backend
        run: npm run build

      - name: Build frontend
        run: cd client && npm run build

      - name: Build Summary
        run: |
          echo "✅ Backend build successful"
          echo "✅ Frontend build successful"
          echo "📦 Ready for deployment"
